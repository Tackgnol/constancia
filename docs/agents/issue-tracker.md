# Issue tracker: GitHub

Issues and PRDs for this repo live as GitHub issues. Use the `gh` CLI for all operations.

Infer the repository from `git remote -v`. Publishing to the issue tracker means creating a GitHub issue. Fetching a relevant ticket means reading the issue and its comments, labels, and current state.
